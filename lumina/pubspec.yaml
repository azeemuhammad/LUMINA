name: lumina_ai
description: Offline AI Photo Enhancer

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image: ^4.1.0
  path_provider: ^2.1.1
  tflite_flutter: ^0.10.4
  confetti: ^0.7.0
  share_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - models/realesrgan_x4.tflite